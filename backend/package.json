{
  "name": "@cybermatters/grc-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --persist-to ./dev-storage",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.22.0",
    "bcryptjs": "^2.4.3",
    "@anthropic-ai/sdk": "^0.17.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.3.0",
    "wrangler": "^3.28.0",
    "vitest": "^1.2.0"
  }
}
